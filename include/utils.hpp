// utils.hpp

#ifndef UTILS_HPP
#define UTILS_HPP

#include <map>
#include <string>
#include <vector>

void list_available_presets(const std::map<std::string, std::vector<std::string>>& presets);

#endif // UTILS_HPP
